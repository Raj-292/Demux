{"id":435607244,"lang":"cpp","time":"5 months, 1 week","timestamp":1609173919,"status_display":"Accepted","runtime":"140 ms","url":"/submissions/detail/435607244/","is_pending":"Not Pending","title":"XOR Queries of a Subarray","memory":"38.8 MB","code":"class Solution {\npublic:\n    vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& query) {\n        // Preprocessing step\n        int n = arr.size(), run_xor = 0;\n        vector<int> prefix(n, 0);\n        \n        // Compute the prefix sum\n        for(int i = 0; i < n; i++) {\n            run_xor ^= arr[i];\n            prefix[i] = run_xor;\n        }\n        \n        // Actual query\n        vector<int> res;\n        for(int i = 0; i < query.size(); i++) {\n            if(query[i][0] == 0)\n                res.push_back(prefix[query[i][1]]);\n            else\n                res.push_back(prefix[query[i][1]]^prefix[query[i][0]-1]);\n        }\n        \n        return res;\n    }\n};","compare_result":"111111111111111111111111111111111111111111","title_slug":"xor-queries-of-a-subarray"}